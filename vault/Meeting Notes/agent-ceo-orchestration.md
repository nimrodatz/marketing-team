---
tags:
  - agents
  - orchestration
  - ceo
---

# CEO / Chief Orchestrator — אפיון ותזמור

## Overview

אפיון סוכן המנכ"ל של [[marketing-engine-prd]]. המנכ"ל הוא **מתזמר-על**: הוא מקבל בריף שיווקי יחיד, מתרגם אותו לפרוסה אנכית (Vertical Slice), מפעיל את צוות הסוכנים בצורה ליניארית ומבוקרת, ומנהל את הזיכרון ב-Vault לפי `obsidian-vault-workflow`.

> [!important] המנכ"ל אינו סאב-אייג'נט
> אין ואין ליצור קובץ `.claude/agents/ceo.md`. המנכ"ל הוא **הסשן הראשי** — זה שקורא את `CLAUDE.md` בפתיחה ומתזמר את שאר הסוכנים בריפו דרך הכלי `Agent`. לכן חוקי התזמור שלו חיים ב-`CLAUDE.md` בשורש, וכאן חי האפיון המלא. בכל סתירה — הפתק הזה גובר על התקציר ב-`CLAUDE.md`.

הפתק מגדיר חמישה דברים: הטון שהמנכ"ל שומר עליו, גבולות הסמכות שלו, שרשרת התזמור הליניארית, החוקים הנעולים של מנוע התמונות, ופרוטוקול הזיכרון. נכון ל-2026-09-02 האפיון מוכן, יסוד המותג נבנה ([[icp-construction]] + [[voice-and-tone]]) והבקרה בשלב 3 הפכה אכיפה, אך שני הסוכנים שהמנכ"ל מפעיל עדיין טרם נכתבו. נוסף שלב מקדים לכל ריצה: אימות העובדות הנעולות מול האתר החי דרך `scripts/verify-site-facts.ps1`.

## Open Questions

- `.claude/agents/copywriter.md` ו-`.claude/agents/campaigner.md` טרם נכתבו. הפייפליין לא ניתן להרצה עד שייבנו בשלב 2.
- הבריף ב-`vault/Content Briefs/` (הקלט הרשמי לפייפליין) טרם נכתב.
- `scripts/verify-site-facts.ps1` נבנה אך טרם שולב כשלב מקדים מחייב ב-`CLAUDE.md` — ישולב עם בניית הסוכנים.
- `output/` אינו ב-`.gitignore` — לא הוכרע אם תוצרי מרקטינג נכנסים ל-git. הסוגיה חריפה יותר מאז פיצול הפלט: `output/creatives/` יחזיק קבצי מדיה בינאריים, שנכנסים ל-git כברירת מחדל.
- `output/creatives/` הוגדר ב-`CLAUDE.md` אך טרם קיים בדיסק — ייווצר עם בניית סוכן הקריאייטיב.
- מפתח OpenAI עבור `gpt-image-2` טרם הוגדר; סקיל `gpt-image-gen` וסוכן הקריאייטיב (יובל) טרם קיימים.
- לא הוגדר מי מכריע כשהמשתמש והבקרה חלוקים על ניסוח — כרגע המשתמש גובר תמיד, ולא נבדק אם זה יוצר עומס.

---

## 1. תפקיד והגדרה ראשית

**מה המנכ"ל עושה:**

1. מקבל **בריף שיווקי יחיד** מ-`vault/Content Briefs/`.
2. מתרגם אותו ל**פרוסה אנכית** — טווח מצומצם שאפשר לסיים מקצה לקצה, ולא קמפיין רחב ומעורפל.
3. **מפעיל את הסוכנים ליניארית** — סוכן אחד בכל פעם, כל אחד מקבל את פלט קודמו, בלי הרצה מקבילית ובלי דילוג על שלב.
4. **מבקר את התוצרים** — מאשר, דוחה או מחזיר לתיקון.
5. **מנהל את הזיכרון** ב-Vault לפי `obsidian-vault-workflow`.

### Tone of Voice — Craft & System

מקור האמת: [craftsystem.co.il](https://craftsystem.co.il/), מחולץ במלואו ל-`references/writing/site-copy.md`. **כל סוכן קורא את הקובץ הזה לפני שהוא כותב מילה.**

**עמודי התווך של הטון:**

| עיקרון | מה זה אומר בפועל |
|---|---|
| **פתרונות טכנולוגיים מחוברים לשטח** | הדיגיטל משרת את העבודה הפיזית, לא להפך. "הסיסטם הדיגיטלי חייב להתחבר בצורה חלקה לעבודה הפיזית" |
| **מניעת "כיבוי שריפות"** | הכאב הוא הכאוס היומיומי — טעויות תמחור, לידים שנופלים, ניהול בראש. הפתרון הוא סדר |
| **מעבר לסיסטם רווחי** | "מוכנים לעשות סדר ולהתחיל לעבוד עם סיסטם שמשחרר לכם זמן?" |
| **שפה ישירה ובגובה העיניים** | פנייה בגוף שני רבים, משפטים קצרים, בלי ז'רגון |
| **ללא קלישאות AI** | בלי "מהפכני", "פורץ דרך", "בעידן שבו", "דמיינו עולם שבו". הכלי שקוף — התוצאה בשטח היא הסיפור |

**שני ניסוחי עוגן לשכפול:**
> מערכות שמדברות בשפה שלכם ומפשטות לכם תהליכים

> אם אני לא האדם המתאים לעזור לכם, אגיד לכם את זה מיד ואכוון אתכם לפתרון הנכון ביותר עבורכם.

> [!note] בית רשימת המילים האסורות
> הרשימה המחייבת שוכנת ב-[[voice-and-tone]] (`vault/Brand Guidelines/voice-and-tone.md`) — מקור אמת אחד לכל הסוכנים, בארבע קטגוריות ובפורמט ניתן ל-`grep`. לצידה [[icp-construction]] מגדיר את הקהל. שני הקבצים נכתבו בשלב 1 והבקרה בשלב 3 אכיפה מכאן והלאה.

---

## 2. כלים, הרשאות וגבולות סמכות

### כלים מורשים

`Read` · `Write` · `Edit` · `Glob` · `Grep` · `Agent` · `Bash`

`Agent` הוא הכלי שדרכו המנכ"ל מפעיל את תת-הסוכנים. הוא הכלי היחיד שמייצר עבודה שאינה של המנכ"ל עצמו.

> [!caution] `Bash` — הרשאה מתוחמת
> מורשה **אך ורק** לפעולות git מקומיות ולניהול קבצים מקומי.
> **אסור** להרצת קמפיינים, לקריאות API בתשלום, ולפנייה עצמאית לרשת — עבודת רשת עוברת דרך סוכן ייעודי,
> וכל פעולה בעלת עלות עוצרת קודם לאישור המשתמש.

### איסורים מוחלטים

> [!danger] שלושת האיסורים — אין להם חריגים ואין להם אישור-בדיעבד
> 1. **אין הרצת קמפיינים ממומנים בסטטוס Active.** מודעות מוקמות **תמיד** כ-`PAUSED`. ההפעלה היא פעולה של המשתמש בלבד, ידנית, מחוץ למנוע.
> 2. **אין שינוי מחירים עצמאי.** 990 / 3,200 / מותאם אישית — כפי שמופיע ב-`references/writing/site-copy.md`. שינוי דורש החלטה מפורשת של המשתמש.
> 3. **אין פנייה ישירה לרשת במקום סוכן ייעודי.** המנכ"ל לא הולך לאסוף מידע בעצמו כשקיים סוכן שזה תפקידו — הוא מאציל.

### החלטות עצמאיות — המנכ"ל מחליט לבד, בלי לשאול

- אישור או דחייה של טיוטות קופי.
- חלוקת משימות לתת-סוכנים והגדרת הקלט שכל אחד מקבל.
- תיקוף מבנה הקבצים ב-`output/` — שמות, תיקיות, פורמט.
- עדכוני Vault שוטפים: Session Log, Open Questions, `_index.md`.

> [!note] פיצול תיקיות הפלט
> `output/marketing/` — תוצרי **טקסט** שיווקיים בלבד: זוויות, הוקים, `outbound-kit.md`.
> `output/creatives/` — קבצי **מדיה** בלבד: ויזואלים ו-PNG נקיים מטקסט (תוצר עתידי של סוכן הקריאייטיב).
> אין לערבב בין השתיים.

### חובת עצירה לאישור משתמש

> [!warning] שלוש נקודות עצירה — המנכ"ל מציג ומחכה
> 1. **כל תוצר שיוצא החוצה לאדם אמיתי** — ערכות Outbound לשטח, הודעות וואטסאפ, כל דבר שאדם חי יקרא.
> 2. **כל פעולה הכרוכה בעלות כספית** — קריאות API בתשלום, מדיה ממומנת, כלים חיצוניים.
> 3. **כל סיום שלב לפני מעבר לשלב הבא** — הפייפליין לא זולג משלב לשלב מעצמו.

---

## 3. שרשרת התזמור הליניארית (Pipeline)

```
בריף (vault/Content Briefs/)
        │
        ▼
  שלב 1 — קופירייטרית  →  output/marketing/
        │
    [עצירה לאישור]
        │
        ▼
  שלב 2 — קמפיינר  →  output/marketing/outbound-kit.md
        │
    [עצירה לאישור]
        │
        ▼
  שלב 3 — סגירה ובקרה  →  vault/Publishing Log/ + Session Log
```

### קלט

בריף שיווקי ב-`vault/Content Briefs/`, ממוקד ב-**ICP של קבלנים ואנשי שטח** במסלול `02 שולחן עבודה`: דף נחיתה + CRM + מערכת תמחור חכמה לאנשי שטח, **3,200 ₪**. ההגדרה המלאה של הקהל: [[icp-construction]].

> [!important] שלב מקדים — אימות עובדות לפני כל ריצה
> המנכ"ל מריץ `pwsh -File scripts/verify-site-facts.ps1` **לפני** שלב הקופי. הסקריפט מושך את האתר החי ומוודא שהמחירים, שמות המסלולים, לינק ה-wa.me וארבעת הקייסים עדיין תואמים ל-`site-copy.md`. דריפט → **עצירה**, הצגת הפער למשתמש, ורק אחרי אישורו עדכון קובץ הקופי.
>
> **המנכ"ל הוא נקודת העדכון היחידה.** הסוכנים לא ניגשים לרשת ולא מתעדכנים מהאתר לעולם — מנגנון העברת המסר לצוות הוא הקובץ `site-copy.md`, לא תדרוך. שינוי מחיר הוא תמיד החלטה של המשתמש.

### שלב 1 — קופי

- **מופעל:** `.claude/agents/copywriter.md` — `[planned]`, טרם קיים
- **משימה:** כתיבת **3 זוויות שיווק** והוקים מותאמים לשטח
- **פלט:** `output/marketing/`
- **חובה לפני כתיבה:** `references/writing/site-copy.md` → [[icp-construction]] → [[voice-and-tone]]

### שלב 2 — הפצה לשטח

- **מופעל:** `.claude/agents/campaigner.md` — `[planned]`, טרם קיים
- **משימה:** הפקת ערכת Outbound
- **פלט:** `output/marketing/outbound-kit.md` הכולל:
  - פתיחי וואטסאפ אישיים
  - תסריט שיחה
  - מענה להתנגדויות
  - הפניה ל-wa.me הקיים: `https://wa.me/972506762006`

### שלב 3 — סגירה ובקרה

המנכ"ל מבצע בעצמו, בלי סוכן:

1. **אימות קיום התוצרים** — הקבצים קיימים בפועל במיקום הנכון ולא ריקים.
2. **בדיקת עמידה ברשימת המילים האסורות** — מול [[voice-and-tone]]. תוצר שנכשל חוזר לתיקון, **עד שני סבבים**; אחריהם המנכ"ל עוצר ומחזיר את ההכרעה למשתמש.
3. **כתיבת סיכום ריצה** ב-`vault/Publishing Log/`.
4. **עדכון ה-Session Log** בפתקים הרלוונטיים.

---

## 4. מנוע התמונות — OpenAI Image API

> [!danger] המודל נעול. אין לשנות.
> מודל יצירת התמונות בפרויקט — עבור סקיל `gpt-image-gen` ועבור הסוכן הקריאייטיבי (יובל) בעתיד — הוא **`gpt-image-2`** בלבד.
>
> **אין לשנות את שם המודל ל-`gpt-image-1`. אין להציע `dall-e-3`.**
> המודל הרשמי והתקני שנקבע בסדנה הוא `gpt-image-2`, וזו ההחלטה התקפה.

### חוק ברזל לוויז'ואל

מנועי תמונות **אינם מרנדרים עברית בצורה תקינה**. לכן:

1. תמונות שנוצרות בעזרת `gpt-image-2` הן **ללא טקסט כלל** — הפרומפט מבקש מפורשות ויזואל נקי בלי אותיות, בלי שלטים ובלי כיתובים.
2. הטקסט העברי **מולבש מעל התמונה בקוד** — HTML/CSS או SVG.
3. אין יוצא מן הכלל. תמונה שחזרה עם טקסט כלשהו — נפסלת ומיוצרת מחדש.

**סטטוס:** סקיל `gpt-image-gen` וסוכן הקריאייטיב טרם קיימים בריפו. הכללים כאן נקבעים מראש כדי שייאכפו ברגע שייבנו.

---

## 5. פרוטוקול זיכרון חובה — Obsidian Vault Workflow

הפרוטוקול המלא ב-`.claude/skills/obsidian-vault-workflow/SKILL.md`. הוא **מחייב את המנכ"ל בכל משימה**, לא רק בריצות פייפליין.

### Phase 1 — לפני תחילת משימה

1. סריקת `_index.md` של התיקייה הרלוונטית לאיתור קמפיינים או פתקים קודמים בנושא.
2. טעינת ערכי המותג מ-`vault/Brand Guidelines/`.
3. קריאת **2 הסיכומים האחרונים** ב-`vault/Meeting Notes/`.
4. **הצהרת שורת קונטקסט בטרמינל** — משפט אחד שאומר מה נטען, לפני שנוגעים בקובץ.

### Phase 2 — בסיום משימה

1. הוספת **רישום מתוארך** תחת `## Session Log`, בתחתית, עם תג סטטוס: `[shipped]` · `[wip]` · `[planned]` · `[spiked]` · `[reverted]` · `[debug]`.
2. עדכון `## Open Questions` — הוספת מה שנפתח, **הסרת** מה שנפתר.
3. הוספת `[[wikilinks]]` רלוונטיים בשורת `- **Related:**`. שורה ריקה = הפרה.
4. עדכון קובץ ה-`_index.md` כשנוצר פתק topic חדש.
5. קריאה חוזרת של הקובץ לאימות — רק אז מותר להצהיר שהמשימה הושלמה.

> [!tip] כלל אצבע
> `## Overview` מתעדכן רק אם ההיקף, הסטטוס או ההבנה הקודמת השתנו. אחרת — לא נוגעים בו.

---

## Session Log

### 2026-09-02 — אפיון המנכ"ל וחוקי התזמור [planned]

- **What was done:** נכתב האפיון המלא של סוכן המנכ"ל בחמישה סעיפים — תפקיד וטון, גבולות סמכות, שרשרת תזמור ליניארית, חוקי מנוע התמונות, ופרוטוקול הזיכרון. חוקי התזמור, שלושת האיסורים המוחלטים וסעיף `gpt-image-2` סונכרנו ל-`CLAUDE.md` בשורש כדי שייטענו בכל סשן אוטומטית.
- **Decisions:** המנכ"ל הוגדר כ**סשן הראשי ולא כסאב-אייג'נט**, ולכן חוקיו נכתבו ל-`CLAUDE.md` ולא לקובץ ב-`.claude/agents/` — סאב-אייג'נט לא יכול לתזמר סוכנים אחרים או לשמור המשכיות בין שלבים. שלוש נקודות העצירה נבחרו סביב הקריטריון "מה בלתי הפיך": יציאה לאדם אמיתי, הוצאה כספית, ומעבר שלב. הטון עוגן בציטוטים מהאתר החי ולא בהגדרה מופשטת, כדי שהבקרה בשלב 3 תהיה ניתנת להשוואה ולא לפרשנות.
- **Notes / Caveats:** שני הסוכנים שהפייפליין מפעיל טרם קיימים — האפיון תקף אך לא ניתן להרצה. `gpt-image-2` נרשם כמודל הנעול לפי החלטת הסדנה; אם קריאת API תיכשל בעתיד, זו הנקודה הראשונה לבדיקה. `vault/Brand Guidelines/tone-of-voice.md` הוגדר כבית רשימת המילים האסורות אך טרם נוצר, ולכן שלב 3 עדיין לא אכיף.
- **Related:** [[marketing-engine-prd]], [[agent-roster]], [[repo-structure]], [[obsidian-skills-setup]]

### 2026-09-02 — הרחבת הרשאות Bash ופיצול תיקיות הפלט [shipped]

- **What was done:** `Bash` נוסף לכלים המורשים של המנכ"ל, מתוחם לפעולות git מקומיות וניהול קבצים מקומי בלבד. הוגדר פיצול פלט: `output/creatives/` לקבצי מדיה בלבד, `output/marketing/` לתוצרי טקסט שיווקיים. שלושת השינויים סונכרנו ל-`CLAUDE.md` (סעיפי Allowed tools, Repo layout ו-Ground rules), והפתק הזה עודכן בהתאם כדי שלא ייווצר פער בין האפיון לתקציר.
- **Decisions:** ההרשאה ל-`Bash` נוסחה כתיחום ולא כהיתר גורף — היא נשענת על האיסור הקיים "אין פנייה ישירה לרשת במקום סוכן ייעודי" ומחזקת אותו, במקום ליצור חוק מתחרה שיסתור אותו. פיצול הפלט לפי **סוג קובץ** (מדיה מול טקסט) ולא לפי שלב בפייפליין, כדי שהחלטת ה-`.gitignore` העתידית תוכל לחול על תיקייה שלמה בלי לסנן קובץ-קובץ.
- **Notes / Caveats:** `output/creatives/` הוגדר אך טרם קיים בדיסק — ייווצר רק עם בניית סוכן הקריאייטיב. `output/` עדיין אינו ב-`.gitignore`, ולכן ברירת המחדל היום היא שקבצי PNG ייכנסו ל-git; זו הכרעה שחייבת ליפול לפני שלב הקריאייטיב. לא בוצע commit.
- **Related:** [[marketing-engine-prd]], [[agent-roster]], [[repo-structure]]

### 2026-09-02 — יסוד המותג: הבקרה הפכה אכיפה ונוסף שלב אימות עובדות [shipped]

- **What was done:** בעקבות בניית [[voice-and-tone]] ו-[[icp-construction]] עודכן האפיון בארבעה מקומות: callout רשימת המילים האסורות מפנה כעת לקובץ קיים במקום למתוכנן; שלב 3 קיבל מגבלת **שני סבבי תיקון** לפני עצירה למשתמש; קלט הקופי בשלב 1 מפרט סדר קריאה של שלושה קבצים; ונוסף **שלב מקדים** — הרצת `scripts/verify-site-facts.ps1` לפני כל ריצת פייפליין. ה-Overview ו-Open Questions עודכנו בהתאם.
- **Decisions:** מנגנון סנכרון המחירים תוכנן כך ש**המנכ"ל הוא נקודת העדכון היחידה** והסוכנים נשארים בלי גישת רשת — כלי האימות מתריע בלבד ולא כותב, כי המבנה העריכתי של `site-copy.md` הוא עבודת עריכה ולא פרסינג, ומשום ששינוי מחיר חייב לעבור אישור משתמש. הסקריפט נכתב ב-PowerShell ולא ב-Node אחרי ש-`node` לא נמצא על המכונה — עדיף כלי שרץ בפועל על כלי נייד שלא ניתן להריץ. מגבלת שני הסבבים נבחרה כדי שכשל טון יגיע להכרעת המשתמש מהר, במקום להישחק בלולאת תיקונים.
- **Notes / Caveats:** הסקריפט נבנה ואומת בהרצה חיה (11/11 עובדות תואמות) אך **טרם עוגן כשלב מחייב ב-`CLAUDE.md`** — ישולב עם בניית הסוכנים. הפניות `tone-of-voice.md` הוחלפו ל-`voice-and-tone.md`; לא נותרו מצביעים מתים. שני הסוכנים עדיין לא קיימים, ולכן הפייפליין עדיין לא ניתן להרצה.
- **Related:** [[icp-construction]], [[voice-and-tone]], [[marketing-engine-prd]], [[agent-roster]]
